// Fallback image used anywhere a hero doesn't have imagen_url set
// (list cards, detail modal, and — once we wire it up — the form preview).
export const DEFAULT_HERO_IMAGE =
  "https://placehold.co/600x800/141416/e62429?text=MARVEL";